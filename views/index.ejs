<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="/output.css" rel="stylesheet" />
  </head>
  <body
    class="bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 text-gray-100 min-h-screen"
  >
    <header class="py-8">
      <h1 class="text-5xl font-extrabold text-center drop-shadow-lg">
        <%= title %>
      </h1>
    </header>

    <nav class="flex justify-center mt-8 space-x-6">
      <% for (let i = 0; i < links.length; i++) { %>
      <a
        href="<%= links[i].href %>"
        class="px-6 py-3 bg-white text-blue-600 font-semibold rounded-full shadow-lg transform hover:scale-105 transition-transform duration-200 hover:bg-blue-600 hover:text-white"
      >
        <%= links[i].text %>
      </a>
      <% } %>
    </nav>

    <main
      class="max-w-5xl mx-auto p-8 mt-10 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8"
    >
      <% messages.forEach(message => { %>
      <div
        class="bg-white p-6 rounded-2xl shadow-lg transform hover:scale-105 transition-transform duration-300"
      >
        <h2 class="text-xl font-bold text-gray-900"><%= message.user %></h2>
        <p class="mt-4 text-gray-700"><%= message.text %></p>
        <p class="mt-6 text-sm text-gray-500">
          Added on: <%= message.added.toDateString() %>
        </p>
        <a
          href="/message/<%= message.id %>"
          class="block mt-4 text-blue-600 hover:text-blue-800 transition-colors"
          >View Message</a
        >
      </div>
      <% }); %>
    </main>
  </body>
</html>
