<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="/output.css" rel="stylesheet" />
  </head>
  <body
    class="bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 text-gray-100 min-h-screen flex items-center justify-center"
  >
    <main class="w-full max-w-xl p-8 bg-white rounded-2xl shadow-2xl">
      <h1 class="text-3xl font-bold text-gray-800 mb-4">
        Message from <%= message.user %>
      </h1>
      <p class="text-xl text-gray-700 mb-6"><%= message.text %></p>
      <p class="text-sm text-gray-500 mb-8">
        Added on: <%= message.added.toDateString() %>
      </p>
      <a
        href="/"
        class="block text-center bg-blue-600 text-white py-3 rounded-full shadow-lg transform hover:scale-105 transition-transform duration-200 hover:bg-blue-700"
        >Back to Home</a
      >
    </main>
  </body>
</html>
