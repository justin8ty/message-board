<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="/output.css" rel="stylesheet" />
  </head>
  <body
    class="bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 text-gray-100 min-h-screen flex items-center justify-center"
  >
    <main class="w-full max-w-xl p-8 bg-white rounded-2xl shadow-2xl">
      <h1 class="text-4xl font-bold text-gray-800 mb-6 text-center">
        <%= title %>
      </h1>
      <form action="/new" method="post" class="space-y-6">
        <div>
          <label for="name" class="block text-gray-800 text-lg"
            >Your Name:</label
          >
          <input
            type="text"
            id="name"
            name="name"
            required
            class="text-gray-800 w-full mt-2 px-4 py-2 border border-gray-300 rounded-full focus:ring-4 focus:ring-blue-500 focus:border-transparent"
          />
        </div>
        <div>
          <label for="msg" class="block text-gray-800 text-lg"
            >Your Message:</label
          >
          <textarea
            id="msg"
            name="msg"
            required
            class="text-gray-800 w-full mt-2 px-4 py-2 border border-gray-300 rounded-lg focus:ring-4 focus:ring-blue-500 focus:border-transparent"
          ></textarea>
        </div>
        <div>
          <button
            type="submit"
            class="w-full py-3 bg-blue-600 text-white font-semibold rounded-full shadow-lg transform hover:scale-105 transition-transform duration-200 hover:bg-blue-700"
          >
            Submit
          </button>
        </div>
      </form>
    </main>
  </body>
</html>
